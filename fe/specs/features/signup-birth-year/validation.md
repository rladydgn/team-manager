# 검증

## 자동 검증

| 검증 | 명령 | 결과 |
| --- | --- | --- |
| Lint | `npm run lint` | 미실행 (사용자 요청에 따라 실행 검증 생략) |
| Build | `npm run build` | 미실행 (사용자 요청에 따라 실행 검증 생략) |

## 확인 항목

- 빈 값과 범위 밖 연도에서 필드 오류가 표시되는지 확인한다.
- 유효한 연도가 `YYYY-01-01` 형식의 `birthDate`로 전송되는지 확인한다.
