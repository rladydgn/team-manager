# 기능 요구사항

## 기능 이름

회원가입 이메일 필수화

## 목표

회원가입 요청에 이메일을 반드시 포함하도록 API 계약, 도메인 검증, 데이터베이스 스키마를 일치시킨다.

## 인수 기준

- 이메일이 누락된 요청은 가입되지 않는다.
- 이메일이 빈 문자열 또는 공백만으로 구성된 요청은 가입되지 않는다.
- 유효한 이메일 값은 공백을 제거한 뒤 저장한다.
- 신규 데이터베이스 스키마의 `users.email`은 `NOT NULL`이어야 한다.
